function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function createPlanets(scene) {
  return notImplemented('createPlanets');
}

export function updatePlanetPositions(planets, jd, lst, latitude) {
  return notImplemented('updatePlanetPositions');
}

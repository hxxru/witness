function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function createTrees(scene, terrain, seed) {
  return notImplemented('createTrees');
}

export function regenerateTrees(trees, terrain, seed) {
  return notImplemented('regenerateTrees');
}

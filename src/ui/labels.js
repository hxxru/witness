function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function createLabels(options = {}) {
  return notImplemented('createLabels');
}

export function updateLabels(labels, state) {
  return notImplemented('updateLabels');
}

function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function createInputPanel(options = {}) {
  return notImplemented('createInputPanel');
}

export function updateInputPanel(panel, state) {
  return notImplemented('updateInputPanel');
}

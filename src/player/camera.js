function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function createPlayerCamera(renderer) {
  return notImplemented('createPlayerCamera');
}

export function updatePlayer(player, terrain, deltaTime) {
  return notImplemented('updatePlayer');
}

function notImplemented(name) {
  throw new Error(`${name} is not implemented yet.`);
}

export function spawnPlayer(player, terrain, landMask, latitude, longitude) {
  return notImplemented('spawnPlayer');
}
